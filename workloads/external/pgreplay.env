WORKLOAD_NAME="pgreplay trace replay template"
WORKLOAD_KIND="compose-run"
WORKLOAD_IMAGE="${PGREPLAY_IMAGE:-postgres:16-alpine}"
WORKLOAD_COMMAND="${PGREPLAY_COMMAND:-printf '%s\n' 'Set PGREPLAY_IMAGE and PGREPLAY_COMMAND for this pgreplay container.' >&2; exit 2}"
