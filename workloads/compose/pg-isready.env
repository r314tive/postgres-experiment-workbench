WORKLOAD_NAME="pg_isready from arbitrary workload container"
WORKLOAD_KIND="compose-run"
WORKLOAD_IMAGE="postgres:16-alpine"
WORKLOAD_COMMAND='pg_isready -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE"'
