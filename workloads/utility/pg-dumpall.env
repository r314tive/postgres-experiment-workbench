WORKLOAD_NAME="pg_dumpall workbench database"
WORKLOAD_KIND="shell"
WORKLOAD_CMD='mkdir -p "$REPO_DIR/logs/utility" && docker compose --env-file "$ENV_PATH" exec -T postgres pg_dumpall -U "$PGUSER" > "$REPO_DIR/logs/utility/pg-dumpall.sql" && test -s "$REPO_DIR/logs/utility/pg-dumpall.sql"'
