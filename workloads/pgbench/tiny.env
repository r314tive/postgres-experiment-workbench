WORKLOAD_NAME="tiny pgbench builtin workload"
WORKLOAD_KIND="pgbench"
PGBENCH_RESET="${PGBENCH_RESET:-1}"
PGBENCH_INIT="${PGBENCH_INIT:-1}"
PGBENCH_SCALE="${PGBENCH_SCALE:-1}"
PGBENCH_CLIENTS="${PGBENCH_CLIENTS:-2}"
PGBENCH_THREADS="${PGBENCH_THREADS:-1}"
PGBENCH_TIME="${PGBENCH_TIME:-5}"
