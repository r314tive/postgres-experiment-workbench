WORKLOAD_NAME="pgbench WAL, checkpoint, and fsync pressure"
WORKLOAD_KIND="pgbench"
PGBENCH_RESET="${PGBENCH_RESET:-1}"
PGBENCH_INIT="${PGBENCH_INIT:-1}"
PGBENCH_SCALE="${PGBENCH_SCALE:-1}"
PGBENCH_CLIENTS="${PGBENCH_CLIENTS:-8}"
PGBENCH_THREADS="${PGBENCH_THREADS:-4}"
PGBENCH_TIME="${PGBENCH_TIME:-60}"
PGBENCH_SCRIPT="workloads/pgbench/scripts/wal-checkpoint-fsync.sql"
