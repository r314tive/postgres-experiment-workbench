# Neutral explicit setting retained so native-toolchain A/B can prove
# within-arm effective-setting stability without requiring a cross-arm GUC
# difference. This is benchmark protocol input, not production guidance.
shared_buffers = '128MB'
